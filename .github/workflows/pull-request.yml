name: Pull Request

on:
  pull_request:
    branches:
      - main
      - "release/**"

permissions:
  contents: read

jobs:
  build-and-test:
    uses: ./.github/workflows/build-test.yml
    with:
      draft-check: true
